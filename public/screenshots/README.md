# Screenshots du dashboard Harmonie Yacht

Dépose ici tes 2 captures d'écran avec exactement ces noms :

- `dashboard-pipeline.png`   → Vue pipeline (leads + pipeline €)
- `dashboard-reservations.png` → Vue réservations (confirmées + suivi)

Format recommandé : PNG, ratio 16:10 (ex. 1600×1000 px), fond clair.
Les images s'affichent automatiquement sur le site une fois déposées.
